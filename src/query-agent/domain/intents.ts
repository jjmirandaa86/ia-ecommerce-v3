/**
 * @deprecated Import from `@/query-agent/domain/tables` for new code.
 * Kept as a stable facade for existing imports.
 */
export {
  ECOMMERCE_INTENTS,
  isKnownIntent,
  type EcommerceIntent,
  type QueryPlan,
} from "@/query-agent/domain/tables";
